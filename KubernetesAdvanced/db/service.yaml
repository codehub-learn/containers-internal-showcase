apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: snm-db
  labels:
    name: db
spec:
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  selector:
    name: db
  type: ClusterIP
