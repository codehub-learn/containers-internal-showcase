apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: snm-db
  labels:
    app: db
    app.kubernetes.io/name: db
spec:
  ports:
    - name: mysql
      port: 3306
  selector:
    app: db
  type: LoadBalancer
