apiVersion: v1
kind: Service
metadata:
  labels:
    app: simpleapp
  name: simpleapp
  namespace: app
spec:
  ports:
  - name: webport
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: simpleapp
  type: ClusterIP
